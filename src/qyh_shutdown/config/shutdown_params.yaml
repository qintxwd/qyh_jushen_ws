# 关机控制参数
qyh_shutdown_node:
  ros__parameters:
    # PLC 连接参数
    plc_ip: "192.168.1.88"
    plc_port: 502

    # 状态发布频率 (Hz)
    publish_rate: 10.0
    
    # Modbus地址配置
    # 关机信号为M100 (Coil地址100)
    shutdown_coil_address: 100
