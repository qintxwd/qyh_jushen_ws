# qyh_jaka_ik_test

**临时测试包** - 验证JAKA SDK多客户端连接支持

## 🎯 测试目的

验证两个ROS2节点能否同时连接到同一个JAKA控制器：
- **节点1**：`qyh_jaka_control` - 主控制节点
- **节点2**：`qyh_jaka_ik_test` - IK求解测试节点

## 🚀 使用方法

### 1. 编译
```bash
cd ~/qyh_jushen_ws/qyh_jushen_ws
source /opt/ros/humble/setup.bash
colcon build --packages-select qyh_jaka_ik_test
source install/setup.bash
```

### 2. 测试多客户端连接

**终端1 - 启动主控制节点：**
```bash
source install/setup.bash
ros2 launch qyh_jaka_control jaka_control.launch.py
```

观察输出：
```
✓ Connected to robot at 192.168.2.200
```

**终端2 - 启动IK测试节点：**
```bash
source install/setup.bash
ros2 launch qyh_jaka_ik_test ik_test.launch.py
```

### 3. 观察结果

#### ✅ 成功场景（支持多客户端）
```
✅ 成功连接到控制器！
⚠️  注意：如果qyh_jaka_control也在运行，
   现在测试的是【多客户端同时连接】
📊 IK统计: 总计=100, 成功=100, 失败=0, 成功率=100.0%
```

**结论**：SDK支持多客户端 → 可以创建独立的IK求解节点

#### ❌ 失败场景（不支持多客户端）
```
❌ 连接失败！错误码: XXX
可能原因：
  3. SDK不支持多客户端连接
```

**结论**：SDK不支持多客户端 → 需要在单节点内集成IK

## 📊 监控测试

**查看IK测试结果：**
```bash
ros2 topic echo /ik_test/result
```

**查看连接状态：**
```bash
ros2 topic echo /ik_test/status
```

**查看统计信息：**
在终端2中每秒会输出IK求解统计。

## 🔧 参数配置

编辑 `config/ik_test_params.yaml`：

```yaml
robot_ip: "192.168.2.200"  # 与qyh_jaka_control相同
ik_test_rate: 100.0        # IK测试频率
auto_connect: true
```

## ⚠️ 注意事项

1. **测试时不要移动机械臂** - 此节点仅调用IK求解，不发送运动指令
2. **网络要求** - 确保PC能ping通192.168.2.200
3. **测试完成后删除此包**

## 🎯 后续计划

- 如果测试成功 → 创建正式的 `qyh_dual_arm_ik_solver` 包
- 如果测试失败 → 在 `qyh_jaka_control` 内集成IK功能
